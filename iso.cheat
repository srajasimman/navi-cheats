% iso, live usb, install, dd, disk image

# write .iso to usb
echo "sudo dd bs=4M if=$(realpath <iso>) of=/dev/$(echo <dev> | awk '{print $1}') conv=fsync oflag=direct status=progress"

$ iso: ls -1 *.iso
$ dev: lsblk -l -n -o NAME,LABEL,MOUNTPOINTS,SIZE,FSUSE%
