% btrfs

$ directory: find . -maxdepth 1 -type d

% btrfs, compression
@ btrfs

# add compression to directory
sudo btrfs property set <directory> compression zstd && sudo btrfs filesystem defragment -fr <directory>

# check compression level of directory
sudo compsize <directory>
