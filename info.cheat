% info, date

# get os install date (not 100% reliable)
stat / | grep Birth

# get time in other time zone
env TZ=<timezone> date

$timezone: find /usr/share/zoneinfo -type f -mindepth 2 -maxdepth 2 -printf '%P\n'
